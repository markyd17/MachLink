flask>=3.0
pyyaml>=6.0
pdfplumber>=0.11
anthropic>=0.34 ; python_version >= '3.8'
# Gives MachLink its own app window (via the OS's WebView2 engine on
# Windows) instead of opening a browser tab. Optional - MachLink falls
# back to the old browser-tab behavior if this isn't installed.
pywebview>=5.0
